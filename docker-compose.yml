version: '3.8'
services:
  radio:
    build: .
    container_name: super-radio
    restart: unless-stopped
    volumes:
      - ./music:/app/music
    environment:
      - TELEGRAM_BOT_TOKEN=${BOT_TOKEN}
      - ADMIN_IDS=${ADMIN_IDS}
