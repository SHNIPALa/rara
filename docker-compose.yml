version: '3.8'

services:
  radio-bot:
    build: .
    container_name: super-radio
    restart: unless-stopped
    network_mode: bridge
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - ./music:/app/music
