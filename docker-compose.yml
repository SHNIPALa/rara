version: '3.8'

services:
  radio-bot:
    build: .
    container_name: super-radio
    restart: unless-stopped
    network_mode: host  # Важно для UPnP (нужен доступ к сети роутера)
    volumes:
      - ./music:/app/music
